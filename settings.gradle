rootProject.name = "omisys"

include(':common:domain')

include(':service:auth:auth_dto')
include(':service:auth:server')

include(':service:config:server')

include(':service:eureka:server')

include(':service:user:user_dto')
include(':service:user:server')

include(':service:gateway:server')